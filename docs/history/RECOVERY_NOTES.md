# 定高起飞旧源码恢复记录

本目录仅用于离线审查，**未接入当前服务，未恢复实飞能力**。

## 来源与范围

- 从本任务 2026-09-11 07:20:26 UTC 的原始新增文件补丁中恢复。
- 文件：`local_takeoff_initial_20260911.py.txt`，共 602 行；保留原代码，未改造。
- SHA-256：`2B6C6E5F73953DA69D8525BA11A2EF3079942B36ECFAD920FD04BFBBBBE4EAD9`。
- 仅找回该模块的初始版本，不能据此认定它就是成功试飞时的完整部署版本；
  当时配置、飞行动作发送器及后续修复还需一并核对。
- 本地 Git 的两个提交及未引用对象中均没有这个起飞模块，不能用 Git 一键恢复整套系统。

## 已核对的区别

早期模块执行“记录本地坐标 → 预发送 → 飞手切 Offboard → 正常 ARM → Z 目标爬升 → 原点悬停”。
发送位置与航向目标，不包含键盘交接代码。它已有地面/DISARM、遥测新鲜度、实体开关、
姿态、超时、爬升及漂移范围检查，并非没有起飞检查。

但原代码不读取光流质量、融合状态或估计器重置计数；运行中的遥测新鲜度检查主要检查时间，
未完整复核位置/速度有限性。坐标重置时缺少作废旧位置目标的处理，也没有新版本的
发送循环长时间停顿保护。因此不能直接用覆盖文件的方式把当前故障保护一并撤回。

当前网页无法启动的一部分原因，是新版本把尚未接入的详细估计器遥测设成了必需条件。
这是现有适配与检查设计不完整，不足以证明传感器本身已损坏；删除判断也不等于修好传感器或定位。

## 当前未执行的操作

没有覆盖 `src` 中的运行代码，没有重启服务，没有修改 PX4 参数、RC 通道或发送任何飞行指令。
本次检查时无人机已落地且 DISARM，但实体 CH6 急停约 1001、网页锁存关闭。
任何后续部署前都应重新确认地面状态及实体急停；历史状态不能替代当时的确认。

恢复工作的验收对象应是“只有定高起飞和持续悬停”的独立基线，先完成离线/仿真与拆桨验证，
保留已发现问题对应的保护，不能把它称为已验证可飞版本。键盘交接继续禁用。
